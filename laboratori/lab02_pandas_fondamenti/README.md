# Laboratorio 2: Pandas - Fondamenti e Manipolazione DataFrame (4 Ore)

## 🎯 Obiettivi
* Caricare dataset Excel multi-scheda con `pd.read_excel`.
* Esplorare metadati e statistiche descrittive (`.info()`, `.describe()`, `.shape`).
* Selezionare colonne e filtrare righe con condizioni logiche multiple (`&`, `|`).
* Utilizzare l'indicizzazione posizionale (`.iloc`) e per etichetta (`.loc`).
* Calcolare vettorialmente nuove colonne economiche e ordinare per trovare i Top Deals.

## 📄 File di Lavoro
* `lab02_esercizi.py`: File con gli esercizi e blocchi `# TODO:` da completare.
* Dataset di riferimento: `dataset/raw/roma.xlsx`.

## 🚀 Esecuzione
```bash
python laboratori/lab02_pandas_fondamenti/lab02_esercizi.py
```
Soluzione di riferimento: `soluzioni/sol02_pandas_fondamenti.py`.
